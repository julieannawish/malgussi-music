말구씨 음악 앱 V3.28 — 자동 매칭 + 없는 곡 자동 삭제

- 재생목록 인덱스는 유지하고 음악 파일은 앱 저장소에 복사하지 않습니다.
- 음악 폴더를 선택하면 현재 재생목록 전체를 폴더와 자동 매칭합니다.
- 파일명+크기 → 정규화 이름+크기 → 유일 파일명 순으로 매칭합니다.
- 선택 폴더에서 찾지 못한 곡은 재생목록 인덱스에서 자동 삭제합니다.
- 재생목록에 '원본 연결 필요' 상태를 기본 표시하지 않습니다.
- 연결되지 않은 곡을 누르면 음악 폴더 선택으로 이동합니다.
- 기존 playlist_index_v321 키는 기존 사용자 데이터 호환을 위해 유지합니다.
